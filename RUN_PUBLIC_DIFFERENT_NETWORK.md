# Public Link Demo

Use one command:

```bash
./run-public.sh
```

At the end, copy the printed `Meeting` link and send it to the other person. Keep the terminal open while testing.

If Cloudflare shows Error 1033, the tunnel terminal was stopped or the link expired. Run `./run-public.sh` again and use the new link.
